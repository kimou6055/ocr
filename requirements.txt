# Base web framework
django>=4.2,<5.0

# PaddleOCR for PP-StructureV3; optional to run OCR
paddleocr>=2.6

# PDF Processing and Table Extraction
pdfplumber>=0.10.3
camelot-py[cv]>=0.11.0
pandas>=2.0.0
openpyxl>=3.1.0
PyPDF2>=3.0.0
pdf2image>=1.16.0

# Image processing for camelot
opencv-python>=4.8.0
ghostscript>=0.7

# Additional dependencies for PaddleOCR may include paddlenlp and torchvision.
# When installing PaddleOCR, refer to their installation guide for the
# appropriate backend (CPU/GPU) and device drivers.