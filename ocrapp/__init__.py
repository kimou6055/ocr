"""
Application configuration for the OCR app.

The ``ocrapp`` app contains the views, forms and URL configuration needed
to allow users to upload scanned documents and extract structured tables
using PaddleOCR's PP‑StructureV3 pipeline. It is registered with Django in
``ocr_project/settings.py``.
"""
